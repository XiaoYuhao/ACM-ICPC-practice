#define _CRT_SECURE_NO_WARNINGS
#include<iostream>
#include<algorithm>
#include<stdio.h>
#include<string.h>
using namespace std;

const int MAX = 20;
char a[MAX], b[MAX];

int main() {
	scanf("%s", a);
	scanf("%s", b);
	int lena, lenb;
	lena = strlen(a);
	lenb = strlen(b);
}